#!/bin/bash

# Ajoute tous les fichiers modifiés au staging area
git add .

# Effectue un commit avec un message spécifié
git commit -m "Changements effectués automatiquement"

# Pousse les changements vers le dépôt distant
git push